Trigger: Microsoft Graph API (Outlook) listening for incoming webhooks.

    Processing: Stateless HTTP request to OpenAI/Anthropic REST API.

    Payload: JSON structured data with dynamic content mapping.

    Output: Real-time routing to Microsoft Teams via Adaptive Cards
